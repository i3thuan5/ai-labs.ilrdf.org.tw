services:
  # nginx-proxy:

  nginx:
    image: nginx:1.28.0-alpine
    ports:
      - 81:80
    volumes:
      - "staticfiles:/staticfiles/"
      - "./nginx/nginx.conf:/etc/nginx/conf.d/default.conf:ro"
    depends_on:
      - gunicorn
      - django-static

  gunicorn:
    build: .
    secrets: &django_secrets
      - django_secret_key
      - postgres_passwd
    environment: &django_envs
      DJANGO_SECRET_KEY: /run/secrets/django_secret_key
      VIRTUAL_HOST: ${VIRTUAL_HOST}
      POSTGRES_PASSWORD: /run/secrets/postgres_passwd
    restart: always
    depends_on:
      postgres:
        condition: service_healthy

  django-static:
    build: .
    command: python manage.py collectstatic --noinput
    restart: no
    volumes:
      - "staticfiles:/staticfiles/"
    secrets: *django_secrets
    environment: *django_envs

  postgres:
    image: postgres:18.0-alpine
    labels:
      - "backup.postgres=true"
    volumes:
      - "postgres-data:/var/lib/postgresql/18/docker"
    secrets:
      - postgres_passwd
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: /run/secrets/postgres_passwd
    healthcheck:
      test: ["CMD", "pg_isready", "-U", "postgres"]
      start_period: "10s"
      start_interval: "1s"
      interval: "10s"
      timeout: "5s"
    restart: always


volumes:
  postgres-data:
  staticfiles:

# network:
  # enable_ipv4
  # enable_ipv6

secrets:
  postgres_passwd:
    environment: POSTGRES_PASSWD_SECRET
  django_secret_key:
    environment: DJANGO_SECRET_KEY

# 問題: nginx設定檔正確到底是愛指向tueh？
#   docker README，設定檔指向「/etc/nginx/nginx.conf」失敗；
#   照教典/etc/nginx/conf.d/default.conf才成功。
# 問題：nginx.conf確實免http directive，毋過官方說明範例有用著http--neh。
# 問題：nginx.conf改過，愛nginx -s reload，新設定才讀會著的款。敢著？
